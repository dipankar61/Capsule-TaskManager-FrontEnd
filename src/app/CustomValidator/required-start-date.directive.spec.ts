import { RequiredStartDateDirective } from './required-start-date.directive';

describe('RequiredStartDateDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredStartDateDirective();
    expect(directive).toBeTruthy();
  });
});
